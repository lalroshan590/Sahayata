
import 'package:flutter/material.dart';

class UtilityOutageScreen extends StatelessWidget {
  const UtilityOutageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Utility Outage Screen'),
    );
  }
}
